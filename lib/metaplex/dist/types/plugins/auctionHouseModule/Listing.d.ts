import { PublicKey } from '@solana/web3.js';
import { Amount, BigNumber, DateTime, Pda } from "../../types";
import { ListingReceiptAccount } from './accounts';
import { TokenWithMetadata } from '../nftModule';
import { Option } from "../../utils";
import { AuctionHouse } from './AuctionHouse';
export declare type Listing = Readonly<{
    model: 'listing';
    lazy: false;
    auctionHouse: AuctionHouse;
    token: TokenWithMetadata;
    tradeStateAddress: Pda;
    sellerAddress: PublicKey;
    bookkeeperAddress: Option<PublicKey>;
    receiptAddress: Option<Pda>;
    purchaseReceiptAddress: Option<PublicKey>;
    price: Amount;
    tokens: Amount;
    createdAt: DateTime;
    canceledAt: Option<DateTime>;
}>;
export declare const isListing: (value: any) => value is Readonly<{
    model: 'listing';
    lazy: false;
    auctionHouse: AuctionHouse;
    token: TokenWithMetadata;
    tradeStateAddress: Pda;
    sellerAddress: PublicKey;
    bookkeeperAddress: Option<PublicKey>;
    receiptAddress: Option<Pda>;
    purchaseReceiptAddress: Option<PublicKey>;
    price: Amount;
    tokens: Amount;
    createdAt: DateTime;
    canceledAt: Option<DateTime>;
}>;
export declare const assertListing: (value: any) => asserts value is Readonly<{
    model: 'listing';
    lazy: false;
    auctionHouse: AuctionHouse;
    token: TokenWithMetadata;
    tradeStateAddress: Pda;
    sellerAddress: PublicKey;
    bookkeeperAddress: Option<PublicKey>;
    receiptAddress: Option<Pda>;
    purchaseReceiptAddress: Option<PublicKey>;
    price: Amount;
    tokens: Amount;
    createdAt: DateTime;
    canceledAt: Option<DateTime>;
}>;
export declare const toListing: (account: ListingReceiptAccount, auctionHouseModel: AuctionHouse, tokenModel: TokenWithMetadata) => Listing;
export declare type LazyListing = Omit<Listing, 'model' | 'lazy' | 'token' | 'tokens'> & Readonly<{
    model: 'listing';
    lazy: true;
    metadataAddress: PublicKey;
    tokens: BigNumber;
}>;
export declare const isLazyListing: (value: any) => value is LazyListing;
export declare const assertLazyListing: (value: any) => asserts value is LazyListing;
export declare const toLazyListing: (account: ListingReceiptAccount, auctionHouseModel: AuctionHouse) => LazyListing;
